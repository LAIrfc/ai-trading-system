# 政策标签人工覆盖（V3.3 Phase 6.4）
# 键：新闻/政策唯一标识（如 时间+标题hash 或 外部ID）；值：人工修正后的标签，次日生效，不追溯历史。
# 格式示例：
# overrides:
#   "20250303_abc123": { "direction": "利好", "influence": 1.2 }
#   "20250302_def456": { "direction": "利空", "influence": 1.0 }
# 策略/数据层读取时先查此表，再回退到自动标注。

overrides: {}

# direction: 利好 | 利空 | 中性
# influence: 0.8 | 0.9 | 1.0 | 1.2 等
